name: publish-gcr

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (e.g. 1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  publish-gcr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'

      - run: gem install glossarist

      - name: Determine version
        id: version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="${{ inputs.version }}"
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Build standard GCR package
        run: |
          glossarist package . -o "isotc204-${{ steps.version.outputs.version }}.gcr" \
            --shortname isotc204 \
            --version "${{ steps.version.outputs.version }}" \
            --title "ISO/TC 204 ITS Vocabulary" \
            --owner "ISO/TC 204"

      - name: Build full GCR package (with formats)
        run: |
          glossarist package . -o "isotc204-${{ steps.version.outputs.version }}-full.gcr" \
            --shortname isotc204 \
            --version "${{ steps.version.outputs.version }}" \
            --title "ISO/TC 204 ITS Vocabulary" \
            --owner "ISO/TC 204" \
            --compiled-formats tbx,jsonld,turtle,jsonl

      - name: Create unversioned aliases
        run: |
          cp "isotc204-${{ steps.version.outputs.version }}.gcr" isotc204.gcr
          cp "isotc204-${{ steps.version.outputs.version }}-full.gcr" isotc204-full.gcr

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: "GCR Package v${{ steps.version.outputs.version }}"
          body: "Auto-generated GCR package containing ISO/TC 204 ITS concepts."
          files: |
            isotc204-${{ steps.version.outputs.version }}.gcr
            isotc204.gcr
            isotc204-${{ steps.version.outputs.version }}-full.gcr
            isotc204-full.gcr
